
samples: tests/test_liqbio_sample.json

reference: autoseq-genome/autoseq-genome.json

params:
    scratch: /tmp/scratch
    bwa:
        threads:
    samblaster:
        rm_dup: true
        threads:
    gatk3:
        jarfile: 
        realigner_target:
            java_options: "--Xmx8G -XX:ParallelGCThreads=10"
            extra: " -allowPotentiallyMisencodedQuals --maxIntervalSize 20000 "
            threads: 
    
